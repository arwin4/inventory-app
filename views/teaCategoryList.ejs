<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Categories of tea</title>
  </head>
  <body>
    <p>
      There are <%= numberOfTeaCategories %> categories of tea in stock
      currently.
    </p>

    <h1>Categories of tea</h1>
    <ul>
      <% allTeaCategories.forEach(category => { %>
      <li>
        <a href="<%= category.url %>"><%= category.name %></a>
      </li>
      <% }) %>
    </ul>

    <button onclick="window.location.href='/catalog/new-category'">
      Add a new category
    </button>
  </body>
</html>
